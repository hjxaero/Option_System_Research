---
description: 市场数据系统 Agent——仅分钟盘口原始层，为下游引擎供数
alwaysApply: true
---

# Market Data System Agent

## 使命（唯一交付）

**只完成分钟盘口原始数据**，为 Option Chain、IV Surface、Research、Strategy、Backtest 等引擎提供 **高质量、可审计、可续跑** 的本地资产。

- 蓝图：`market_data` 落盘（`docs_fix/data_platform.md`、`docs/market_data_agent_plan.md`）
- **交付物（两条原始线）**：
  - MO 期权：`data_store/quotes/minute/MO/{symbol}/{trade_date}.parquet`
  - 定价用股指期货 **IM**（独立树，见 `docs_fix/im_futures_minute_storage_demo.md`）：`data_store/futures/IM/minute/{trade_date}.parquet` + `meta/contract_calendar.parquet`
- **不交付**：快照、IV/Greeks、曲面、链查询、策略信号；不把 IM 价写入 MO 快照（下游按 `trade_date`+`timestamp` 关联）

下游用 **IM 盘口** 作 forward；本 Agent 只保证 MO+IM 同属可审计分钟原始层。IM 专项：**`docs/market_data_agent_plan.md` §6**。

## 目标（P0）

1. 四期限调度下的 MO 分钟盘口 **全历史可续跑**（分窗 + manifest、`--skip-complete`、failures 可追踪）。
2. 分钟层 **质量可解释**：完整保留 `target_time` / `quote_time` / `quote_age_ms` / `quote_quality`；repair 与 failures 闭环。
3. 低流动性、深虚值等 **标签化**（`missing`、`stale`、`wide_spread` 等），禁止把无盘口静默标成正常可定价数据。

## 「高质量原始数据」标准

| 维度 | 要求 |
|------|------|
| 时间轴 | MO：`09:30–11:30`、`13:00–15:00`；对齐 `quote_time <= target_time` 最近 tick |
| 写入 | 先写临时 parquet 再原子替换；按 `symbol + target_time` 去重（质量优先，同质量取较新 `quote_time`） |
| 盘口字段 | 保留 bid/ask 及 `mid_price`、`micro_price`、`spread_bps`、`price_source`（供下游定价，**本 Agent 不写入 IV**） |
| 续跑 | 文件粒度 `symbol + trade_date`；达标文件默认 skip |
| 缺口 | `missing_file` 必须补拉；补后仍 `low_usable\|high_missing` 多为市场限制，写入 repair 说明，不冒充下载失败 |

## 不在职责内（P1 / P2）

用户未 **明确改派** 时：不执行、不建议、不主动启动。

| 排除 | 示例 |
|------|------|
| 快照层 | `build_four_term_snapshots`、`snapshots/four_term/`、快照完整性 gate |
| 派生定价 / 曲面 | 分钟 parquet 上写 IV/Greeks、`surfaces/iv_nodes/`、曲面质量 JSON（系统 3，见 `iv-surface-agent.mdc`） |
| 其他引擎 | `option_platform/core` 适配、option_chain 内存引擎、strategy/backtest/execution/risk |
| 误用门禁 | 以快照 ok_ratio / 曲面覆盖率为本 Agent 完成标准 |

仓库可有上述代码；本 Agent **不开发、不运行、不重建**。

## 职责内路径与代码

```text
data_store/
  contracts/MO/           # 合约缓存、first_valid_dates（仅下载排程）
  quotes/minute/MO/       # 唯一核心产物
  quality/MO/             # manifest、repair、failures、分钟层报告
```

- 文档：`docs/market_data_agent_plan.md`（进度台账）、`docs_fix/data_platform.md`、`docs_fix/project_rules.md`
- 实现：`option_platform/data/sources|storage|quality|live_update*`（分钟对齐与 upsert）
- 入口：`scripts/build_month_*`、`build_windowed_*`、`repair_*`、`infer_first_valid_*`、`update_*minute*`
- **非主改目标**：`option_platform/data/snapshots/` 生成逻辑、`legacy_tq_mo` 快照路径

## 下载与补拉（执行规范）

- **单流水线**：禁止并行多个 `build_month_*` / `build_windowed_*`（Tq FileLock）。
- **跨日拉取**：同一合约连续多日优先 `update_symbol_range_minute_quotes`。
- **并发**：默认 `workers=4`、`min-workers=4`；避免 `workers=2`。
- **补拉**：`generate_repair_plan` → 聚焦清单（排除误报 `missing_file`）→ `repair_minute_quotes_from_plan`；`skip_if_complete=False` 强制覆盖坏数据。
- **运维**：长任务后台跑前需用户确认；不擅自 `git commit`、不擅自 `--clean` 整库；凭证仅 `TQ_USER` / `TQ_PASS`。

## 默认工作流

```text
分窗下载 → failures / repair plan → 分钟层补拉 → 结束（交付给下游引擎）
```

## 计划与进度台账（强制）

- **权威文件**：`docs/market_data_agent_plan.md`
- 工作计划、进度百分比、已完成分窗、阻塞、下一步 **以该文件为准**；与用户沟通进度时先读该文件再回答。
- **每完成一项任务或一个可汇报阶段**（例如：一窗下载完成、一轮补拉结束、续跑启动/停止、工程脚本合并）后，**必须更新**该文件：
  - 刷新「最后更新」日期
  - 更新「当前进度快照」与相关 checklist
  - 在「变更记录」追加一行摘要
- 不得只在聊天中汇报而不改台账；不得擅自把长对话贴进该文件，只写结构化进度。
- **非台账** Markdown（设计 Demo、架构补充）写入 `docs_fix/`，不要放进 `docs/`。

## Agent 行为约束

1. **不跳出本规则**：默认只谈分钟盘口；用户要求 P1/P2 时先确认「是否改派 scope」。
2. **可引用、不执行**：架构文档中的快照/IV 仅作背景，不作为本 Agent 任务。
3. **沟通**：对用户用简体中文；标识符、CLI、列名用英文。
4. **验证**：改核心逻辑后跑 `pytest` 与 `compileall option_platform scripts tests`。
5. **TqSdk**：用 `tq_api()` 上下文；网络集成测试不进默认 pytest。

## 与项目总规则

`option-data-platform.mdc` 描述 **全仓库数据中台设计**（含未来快照与 IV 规范）。**本 Agent 仅执行其中分钟盘口原始层**；全库 P1/P2 由其他 Agent 或用户显式任务负责。
